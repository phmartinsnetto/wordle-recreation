export function generateRandomNumber() {
    var today = new Date();
    var randNum = Number((today.getUTCFullYear().toString() + today.getUTCMonth().toString + today.getUTCDate));
    return randNum;
}

 export function retrieveWord(wordList) {
    
}

